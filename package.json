{
  "name": "overclockingheroes",
  "private": true,
  "description": "pnpm monorepo root for OverClockingHeroes — shared node_modules for all packages",
  "scripts": {
    "dev": "pnpm --filter site dev",
    "build": "pnpm --filter site build",
    "preview": "pnpm --filter site preview"
  },
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.25.0",
      "minimatch": ">=10.2.1"
    }
  }
}
